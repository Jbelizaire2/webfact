{%   extends 'Main/base_test.html.twig'    %}  

{% block body %}
   
           <div class="box box-primary">
            <div class="box-header with-border">
              <h3 class="box-title">CREAR IMAGEN</h3>
            </div>
				
	
              {{form_start(form, { 'enctype':"multipart/form-data"})}}


              <div class="box-body">
              <div class="col-md-6">


          
              <div class="form-group">
              {{ form_label(form.tituloImagen) }}
			        {{ form_widget(form.tituloImagen, {'attr': {'class': 'form-control'}}) }}
			        <span class="text-danger">{{ form_errors(form.tituloImagen) }}</span>
              </div>



              <div class="form-group">
              {{ form_label(form.descripcion) }}
			        {{ form_widget(form.descripcion, {'attr': {'class': 'form-control'}}) }}
			        <span class="text-danger">{{ form_errors(form.descripcion) }}</span>
              </div>
            <div class="form-group">
              {{ form_label(form.url) }}
			        {{ form_widget(form.url, {'attr': {'class': 'form-control'}}) }}
			        <span class="text-danger">{{ form_errors(form.url) }}</span>
              </div>

              <div class="form-group">
              {{ form_label(form.archivo) }}
			        {{ form_widget(form.archivo, {'attr': {'class': 'form-control'}}) }}
			        <span class="text-danger">{{ form_errors(form.archivo) }}</span>



        <br>
			      <p>

			    	<input type="submit" class="btn btn-success " value="Guardar" id="guardarx1" onclick='buswa()'>

		        </p> 

              {{ form_end(form) }}

              
             <ul>
                  <li>
                    <a href="{{ path('mantenedor_image_index') }}">Volver a Lista</a>
                </li>
          	</ul>  

              </div>
           </div>


          </div>

{% endblock %}

{% block javascripts %}
<script type="text/javascript">
	$(function() {

	});


    function buswa(){
let archivo = $("#appbundle_imagen_archivo").val();
   let extensiones = archivo.substring(archivo.lastIndexOf("."));
   if (archivo=="") {
   swal("El archivo no puede estar vacio!");
    return false;
   }else if(extensiones != ".jpg" && extensiones != ".pnf" && extensiones != ".jpeg")
{
    swal("El archivo de tipo " + extensiones + "  no es válido");
    return false;
}
		// alert(" click guardar");

		$('[name=appbundle_imagen]').submit(); 

   

	}
  


</script>

{% endblock %}